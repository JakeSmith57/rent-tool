# Regenerated by the ETL scripts -- don't commit, they're large and
# derived, not source.
data/raw/*.csv
data/raw/*.json
data/interim/*.db
data/interim/*.csv
data/processed/*.csv

# Real DHCR PDF / other large manual downloads
data/raw/*.pdf

# Never commit secrets
.env
*.key

# Python
__pycache__/
*.pyc
geo/build_geo_output.json
geo/parks_raw.json
geo/build_geo_output.json
geo/parks_raw.json
geo/build_geo_output.json
geo/parks_raw.json

# Exception: derived from the manually-downloaded DHCR PDF, which the
# Actions workflow cannot fetch itself -- so this one parsed output is
# committed rather than regenerated. All other data/ CSVs stay ignored.
!data/interim/dhcr_brooklyn_parsed.csv
